en:
  lita:
    handlers:
      bitbucket_pipelines:
        initiated: "%{user} triggered build %{repo}:%{build} \n%{url}"
        successful: "Build %{repo}:%{build} Success :parrot:"
        error: "%{repo}:%{build} Failed \n%{url}"
